#include <iostream>
#include <vector>

using namespace std;

int main() {
    int n;
    int m;
    cin >> n >> m;

    int a, b;

    vector<int> friends1;
    vector<int> friends2;

    while (cin >> a >> b) {
        friends1.push_back(a);
        friends2.push_back(b);
    }

    for (int i = 0; i <= m / 2; i++) {
        if (friends1[])
    }
    vector<int> remaining;

}